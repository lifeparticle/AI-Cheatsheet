# Copy to .env. The Agent SDK uses the bundled Claude Code CLI; set your API key if
# you authenticate that way: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Optional opaque id for Messages API metadata.user_id (abuse tracing); use a uuid or hash.
# TIMELINE_USER_ID=
